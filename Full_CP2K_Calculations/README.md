How to Extract Archived Data:

Some calculation outputs are stored as compressed archives (.tar.gz) to simplify transfer and storage.  
Run the step below to unpack them in Linux:

tar -xzf archive_name.tar.gz
